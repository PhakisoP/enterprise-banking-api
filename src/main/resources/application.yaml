spring:
  application:
    name: enterprise-banking-api

  datasource:
    url: jdbc:mysql://localhost:3306/enterprise_banking
    username: root
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    baseline-on-migrate: true